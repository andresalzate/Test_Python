# 💎 Test_Python


### 1) Install

            $ sudo easy_install pip
            $ pip install behave
            $ pip install selenium

### 2) Execute scenarios

Default:

            $ behave google_search.feature
